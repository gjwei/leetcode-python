class Solution(object):
    def findCircleNum(self, M):
        """
        :type M: List[List[int]]
        :rtype: int
        """
        friends = range(len(M))
        n = len(M)
        for i in xrange(n):
            for j in xrange(i+1, n):
                if M[i][j] == 1:
                    
    def find()

